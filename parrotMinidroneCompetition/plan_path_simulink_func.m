function [landing_zone_detected, takeoff_complete, target_x, target_y] = plan_path_simulink_func(current_pos, vision_data)
%PLAN_PATH_SIMULINK_FUNC Provides navigation point in traking stage and determines if the landing zone is detected
%   Detailed explanation goes here

    landing_zone_detected = 0;
    takeoff_complete = 0;
    target_x = 0;
    target_y = 1;

end

